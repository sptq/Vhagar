<%- title("Edycja pomieszczenia #{@room.title}") -%>

<% content_for :form_buttons do %>
	<%= link_to 'Anuluj', rooms_path, class: "ui red button" %>
	<%= link_to 'Zobacz', @room, class: "ui teal button" %>
<% end %>

<%= render 'form' %>